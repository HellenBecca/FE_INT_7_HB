const sideLength = prompt("Enter the length of one side of the square:"); // Request input from the user
const result = sideLength * 4; // Calculate the perimeter and store it in 'result'
alert(`The perimeter of the square is ${result}`); // Display the result
